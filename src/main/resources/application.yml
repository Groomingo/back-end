spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/groomingo
    username: wuchangb
    password: ckddn951753
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true